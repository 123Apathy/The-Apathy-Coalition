# DreamTeam Backlog

## Done (2026-06-18 rebuild — template → system)
- Split the shared operating standard into _SYSTEM.md (read once, no longer
  duplicated across 32 files).
- Gave every expert a machine-readable contract (YAML frontmatter: owns,
  excludes, inputs, outputs, kpis, depends_on, escalates_to, cadence).
- Filled every expert with real content: purpose, role-specific principles,
  numeric KPIs, decision gate, failure modes, a worked example, tooling, glossary.
- Resolved overlapping roles with explicit boundaries (visual vs graphic;
  ux-researcher vs human-behavioural-scientist vs psychologist; pricing vs
  billing vs upsell vs analytics; legal vs risk-governance vs security).
- Added ROSTER.md (registry + routing table) and ORCHESTRATION.md (engagement
  loop, quorum-based conflict resolution, decision records, script library).
- Added RUBRIC.md (per-expert + panel scoring) and scripts/validate.mjs
  (enforces the contract; fails on placeholders or broken references).
- Wired the memory loop to ../memory/embeddings/expert-learnings.json.

## Next — earn production (90+) through real use
- Run real engagements; replace worked examples' modeled numbers with measured
  outcomes. (A file caps at 89 until it carries one real, dated learning.)
- After each engagement, append a learning to expert-learnings.json so lessons
  propagate across the panel.
- Capture production edge cases into each expert's Failure Modes.
- Re-validate KPIs/benchmarks quarterly against market + regulatory drift.

## Maintenance
- Run `node DreamTeam/scripts/validate.mjs` before committing changes to experts.
- Quarterly: review roster boundaries; merge or split roles as the work demands.
- Keep the routing table in ROSTER.md in sync when experts are added/removed.
